# FedEx 自动报价系统 (Automated Quoting System) - 架构设计

## 1. 核心需求
- **准确报价**：基础运费 + 附加费用 + 信用卡返点。
- **配置解耦**：每年变动的公布价、折扣、返点必须模块化配置，**不可硬编码**在底层代码中。
- **易于维护**：提供界面或入口供非技术人员（运营/业务）修改年度参数。

## 2. 系统分层架构

### 2.1 数据与配置层 (Data & Config Layer)
负责存储所有易变的费率与参数。
- **FedEx 公布价矩阵 (Base Rates)**: [计费重量] x [Zone分区] 的二维表（支持 Excel/CSV 导入）。
- **附加费规则与标准库 (Surcharges & Rules)**: 
  每一项附加费都需要配置独立的**触发条件 (Trigger Conditions)** 和 **收费标准 (Rates)**。
  - *尺寸/重量型触发*：如 AHS(附加处理费) 配置为 `实重 > 50 lbs` 或 `最长边 > 48 inch`。
  - *布尔型触发*：如 Residential(住宅费) 由前端传入的 `is_residential = True` 触发。
  - *地区型触发*：如 DAS(偏远地区)、DAS Extended(超偏远) 或 Remote(极偏远)，通过比对联邦快递官方提供的年度偏远邮编数据库 (例如 `DAS_Contiguous_Extended_Remote_Alaska_Hawaii_2025.xlsx`) 自动判定并触发对应级别的附加费。
  - *全局型触发*：如 燃油附加费(Fuel Surcharge) 默认触发，按基础运费+特定附加费的比例收取。
- **合作折扣与返点参数 (Discounts & Rebates)**:
  - 基础运费折扣率
  - 各项附加费的独立折扣率
  - 信用卡返点比率
  - 抛重系数 (DIM Divisor，250)
- **版本控制**: 支持按年份/生效日期保存不同版本的费率表。

### 2.2 核心计算引擎 (Pricing Engine)
系统的底层代码，保持逻辑稳定。
1. **输入解析**: 获取包裹长、宽、高、实重、目的地、服务类型。
2. **计费重量判定**: `Chargeable Weight = Max(Actual Weight, DIM Weight)`
3. **基础运费模块**: 根据(计费重量, Zone)查表获取公布价 -> 应用基础折扣。
4. **附加费规则引擎 (Surcharge Rule Engine)**: 
   - **条件匹配**：将包裹属性（长、宽、高、实重、邮编、地址类型）输入规则引擎，遍历附加费规则库。
   - **触发计算**：仅当包裹满足特定附加费的“触发条件”时，才提取该项附加费公布价。
   - **折扣应用**：对命中的附加费分别乘以相应的折扣率。
5. **总账单计算**: 账单总费用 = 折后基础运费 + 折后附加费。
6. **返点计算模块**: 基于最终的“账单总费用”乘以信用卡返点比率，得出返点金额。
7. **最终成本核算**: 最终实际成本 = 账单总费用 - 返点金额。

### 2.3 管理后台与交互 (Admin & UI)
- **报价终端 (User Interface)**: 提供给用户使用的交互界面。
  - **包裹参数输入**：长、宽、高、实重、目的地 Zip Code。
  - **布尔条件选项**：如“是否住宅区 (Residential)”、“是否需要签名”等单选按钮或复选框。
- **配置与费率更新模块**: 
  - 提供界面供上传新一年的 FedEx Excel 费率表 (如 Base Rates 模板)。
  - 提供界面上传及解析年度附加费规则表（如 DAS 邮编数据库）。
- **参数调节器**: 提供 UI 表单即时调整各项折扣百分比、返点率等。

## 3. 建议技术栈
- **核心逻辑**: Python + Pandas (擅长矩阵查表与复杂规则计算)
- **Web 框架 / UI**: Streamlit (极其适合快速开发此类带配置项的数据计算内部工具)
- **数据持久化**: SQLite / 本地 CSV 体系
